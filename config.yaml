meta:
  engine: 4.1.0
units:
  u: 16.5
  pinky_stagger: 5u/4
  pinky_splay: 5
  ring_splay: 3
  thumb_distance: 14 
  reachy_splay: 5
points:
  rotate: pinky_splay + ring_splay
  key:
    width: u
    height: u
    autobind: 0
  zones:
    matrix:
      rows:
        bottom:
        home:
        top:
      columns:
        pinky.rows.bottom.skip: true
        ring.key:
          stagger: pinky_stagger
          splay: -pinky_splay
          origin: [-u/2, -u/2]
        middle.key:
          stagger: u/4
          splay: -ring_splay
          origin: [-u/2, -u/2]
        index.key.stagger: -u/4
        inner.key.stagger: -u/2
        inner.rows.bottom.skip: true
        pinky.rows.top.bind: [0, u, 0, 0]
        ring.key.bind: [0, u, 0, 0]
        ring.rows.bottom.bind: [0, u, 0, u/2]
        index.rows.bottom.bind: [0, 0, 0, u]
    thumb:
      anchor:
        ref: matrix_inner_bottom
        shift: [0, -thumb_distance]
      columns:
        tucky:
        reachy.key:
          stagger: 0
          splay: -5
          origin: [-u/2, -u/2]
        tucky.key.bind: [2u, u/2, 0, 0]
outlines:
  keys:
    - what: rectangle
      size: u
      where:
        - /matrix/
        - /thumb/
      operation: stack
  board:
    - what: rectangle
      size: u
      where:
        - /matrix/
        - /thumb/
      bound: true
      expand: 2
      joints: 2
pcbs:
  hummingbird:
    template: kicad8
    outlines:
      main:
        outline: board
      keys:
        outline: keys
        layer: User.Comments
